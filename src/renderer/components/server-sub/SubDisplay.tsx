import { MDBCol } from "mdb-react-ui-kit";
import React from "react";

const SubDisplay = React.memo((props: any) => {
  return (
    <MDBCol md={9} className='sub-display'>
        hello
    </MDBCol>
  )
})

export default SubDisplay;
